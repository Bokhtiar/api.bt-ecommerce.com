import * as login from "./login.validators";
import * as category from "./category.validators";
import * as register from "./register.validators";
export const validator = {
  login,
  category,
  register,
};
